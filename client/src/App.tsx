// Neon Omatsuri Cabinet: this route intentionally holds only the full-screen playable cabinet.
import GameCanvas from "@/components/GameCanvas";

function App() {
  return <GameCanvas />;
}

export default App;
